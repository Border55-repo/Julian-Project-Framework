{
  "name": "julian-project-framework",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-project.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
